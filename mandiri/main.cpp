#include <iostream>

using namespace std;

const nMax = 50;
struct lemari {
    int baju[nMax];
    int maks;
    int no_laci;
};

void reversed_view(lemari C) {

    for(int i=C.no_laci; i >= 0; i--) {
        cout << ?
    }
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
